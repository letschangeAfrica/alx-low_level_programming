#include <stdio.h>
#include <stdlib.h>

/**
 * mod - A function that divides two integers.
 * @a: integer number
 * @b: integer number
 *
 * Return: Result of the modulo.
*/

int mod(int a, int b)
{
        return (a % b);
}
