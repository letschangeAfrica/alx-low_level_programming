#include <stdio.h>
#include <stdlib.h>

/**
 * mul - A function that adds two integers.
 * @a: integer number
 * @b: integer number
 *
 * Return: Result of the addition.
*/

int mul(int a, int b)
{
	return (a * b);
}
