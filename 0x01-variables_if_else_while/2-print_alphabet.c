#include <stdio.h>

/**
 * main - A program that prints the alphabet in lowercase
 *
 * Return: 0 Always (Success)
 */

int main(void)
{
int i;
for (i = 'a'; i <= 'z'; i++)
putchar(i);
putchar('\n');
return (0);
}
